// Package v2 provides a phase-separated merge engine for Windows Registry hive files.
// It is the successor to the v1 merge engine (hive/merge), achieving sub-5ms merges
// through phase separation, PatchTrie, and bump allocation.
package v2
